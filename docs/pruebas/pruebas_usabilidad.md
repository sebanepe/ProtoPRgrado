# Pruebas de usabilidad

- Verificar que la página de Settings muestra y permite actualizar `ModelConfig`.
- Verificar que el endpoint `/settings/model-config` responde con la configuración activa.
- Verificar flujo: subir dataset → evaluar modelos → seleccionar modelo activo → generar alertas.
